#include <stdio.h>
#include <stdtype.h>

int main(void) {
  f32 a = 1.5f;
  f32 b = 3.75f;
  double c = 2.25;
  double d = c + 1.5;

  if (!(a < b) || !(d > c))
    return 1;

  printf("%.1f %.2f\n", a, b);
  printf("%.2f %.2f\n", -b, c);
  printf("%.2f\n", d);
  return 0;
}
